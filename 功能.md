# FMS 功能路线整理

## 当前判断

FMS 现在已经具备一个简易文件系统的核心雏形：可以格式化、挂载、创建目录、创建文件、切换目录、查看目录、读写文件、追加文件、删除文件和删除目录。现有测试也已经覆盖了部分数据块分配、文件读写持久化、间接块和删除回收。

因此，下一步不建议继续直接堆更多 shell 命令，例如 `vim`、`cp`、`mv`、`chmod`、`useradd`。更合适的方向是先把已有能力整理成更标准、更可扩展的文件系统核心。

最重要的缺口是：

```text
login / logout 尚未接入用户会话
open / close 尚未接入文件描述符模型
write / cat / append 现在主要还是按路径直接操作
后续权限、vim、cp、mv 都缺少统一底层支撑
```

## 下一步优先级

### 1. 先实现 open / close 和 fd 模型

这是当前最应该优先做的事。

建议在 `FileSystem` 中加入运行态：

```python
current_user
open_file_table
next_fd
```

建议新增打开文件对象：

```python
class OpenFile:
    fd: int
    inode_id: int
    path: str
    mode: str
    offset: int
    readable: bool
    writable: bool
```

建议新增核心 API：

```python
fs.open(path, mode) -> int
fs.read(fd, size=-1) -> bytes
fs.write(fd, data) -> int
fs.seek(fd, offset, whence=0) -> int
fs.close(fd) -> None
```

这样当前已有命令可以逐步改成复用 fd 模型：

```text
cat file       -> open(file, "r") + read(fd) + close(fd)
write file x   -> open(file, "w") + write(fd, x) + close(fd)
append file x  -> open(file, "a") + write(fd, x) + close(fd)
```

这样做的收益：

- `cat/write/append` 的底层逻辑统一。
- 后续 `vim` 可以复用 `open/read/write/close`。
- 后续 `cp` 可以复用 `open/read/write/close`。
- 后续权限检查可以集中放在 `open` 阶段。
- 多个打开文件可以拥有独立 offset，更接近真实文件系统语义。

### 2. 为 fd 模型补测试

实现 fd 之前和之后，都要守住现有持久化链路。

建议新增测试：

```text
format -> mount -> mkdir -> touch -> write -> cat -> remount -> cat
open -> write -> close -> remount -> read
append 是否从文件末尾写入
seek 后 read/write 是否使用正确 offset
close 后 fd 是否失效
多个 fd 是否独立维护 offset
用只读模式打开后 write 是否报错
用只写模式打开后 read 是否报错
```

验收标准：

```text
文件内容在 close 后写回磁盘。
重新 mount 后仍能读到正确内容。
fd 关闭后不能继续使用。
```

### 3. 再做最小用户会话

不要一开始就做完整权限系统。建议先做最小闭环：

```text
format 时创建 root 用户
login root
whoami
logout
```

建议新增 API：

```python
fs.login(username, password) -> None
fs.logout() -> None
fs.whoami() -> str
```

建议新增 shell 命令：

```text
login username
logout
whoami
```

验收标准：

```text
mount 后默认可以是未登录状态或 root 状态，但要明确。
login 成功后 current_user 改变。
logout 后 current_user 清空或回到默认状态。
whoami 能显示当前用户。
```

### 4. 用户系统稳定后，再做 useradd 和 home 目录

最小用户会话稳定后，再扩展：

```text
useradd alice
passwd alice
users
su alice
```

建议目标：

```text
root 可以创建用户。
创建 alice 时自动创建 /home/alice。
alice 登录后默认工作目录是 /home/alice。
alice 创建的文件 owner_id 是 alice 的 user_id。
重新 mount 后用户表和 home 目录仍然存在。
```

### 5. 最后再接入权限模型

权限系统会影响所有公开 `FileSystem` API，不适合太早做。

建议字段：

```text
owner_id
group_id
mode
```

建议命令：

```text
chmod 755 dir
chmod 644 file
chown user file
chgrp group file
```

权限检查建议放在 `FileSystem` 核心层，而不是 shell 层。

典型规则：

```text
ls /dir          需要目录 r 和 x
cd /dir          需要目录 x
touch /dir/a     需要父目录 w 和 x
mkdir /dir/sub   需要父目录 w 和 x
open file "r"    需要文件 r
open file "w"    需要文件 w
root 用户可以绕过普通权限限制
```

## 推荐实现顺序

```text
1. open / close / fd / open_file_table
2. read(fd) / write(fd) / seek(fd)
3. 让 cat / write / append 复用 fd 模型
4. 补 fd 和重挂载相关测试
5. login / logout / whoami 最小版本
6. useradd / passwd / users / su
7. /home/<user> 和文件 owner_id
8. chmod / chown / chgrp
9. cp / mv / rename / stat
10. vim / ln / sync / cache
```

## 命令完成情况

### 核心命令

| 序号 | 命令 | 对应功能 | 当前情况 | 建议 |
| --- | --- | --- | --- | --- |
| 1 | format | 格式化磁盘 | 已完成 | 保持并补重挂载测试 |
| 2 | mount | 挂载磁盘 | 已完成 | 保持并补错误镜像测试 |
| 3 | mkdir | 创建目录 | 已完成 | 后续接入权限检查 |
| 4 | cd | 切换目录 | 已完成 | 补相对路径、`.`、`..` 测试 |
| 5 | ls | 查看目录 | 已完成 | 后续显示类型、权限、大小 |
| 6 | touch | 创建空文件 | 已完成 | 后续设置 owner/mode |
| 7 | cat | 读取文件 | 已完成 | 改为复用 open/read/close |
| 8 | write | 写文件 | 已完成 | 改为复用 open/write/close |
| 9 | append | 追加文件 | 已完成 | 改为复用 open/seek/write/close |
| 10 | rm | 删除文件 | 已完成 | 继续加强资源回收测试 |
| 11 | rmdir | 删除目录 | 已完成 | 继续加强递归删除测试 |
| 12 | open | 打开文件 | 未完成 | 下一步优先实现 |
| 13 | close | 关闭文件 | 未完成 | 和 open 同步实现 |
| 14 | login | 登录 | 未完成 | fd 模型后实现 |
| 15 | logout | 登出 | 未完成 | fd 模型后实现 |

### 扩展命令

| 序号 | 命令 | 对应功能 | 当前情况 | 建议 |
| --- | --- | --- | --- | --- |
| 1 | pwd | 打印当前目录 | 已完成 | 保持 |
| 2 | clear | 清屏 | 已完成 | 保持 |
| 3 | help | 帮助 | 已完成 | 随功能更新 |
| 4 | whoami | 当前用户 | 未完成 | login 后优先做 |
| 5 | useradd | 新增用户 | 未完成 | 用户会话稳定后做 |
| 6 | passwd | 修改密码 | 未完成 | useradd 后做 |
| 7 | users | 查看所有用户 | 未完成 | useradd 后做 |
| 8 | su | 切换用户 | 未完成 | login/logout 稳定后做 |
| 9 | chmod | 修改权限 | 未完成 | owner/mode 接入后做 |
| 10 | cp | 复制文件 | 未完成 | fd 模型稳定后做 |
| 11 | mv | 移动文件 | 未完成 | 目录 entry 逻辑稳定后做 |
| 12 | rename | 重命名 | 未完成 | 可和 mv 合并实现 |
| 13 | stat | 文件状态 | 未完成 | 权限前后都值得做 |
| 14 | vim | 编辑文件 | 未完成 | fd/read/write 稳定后做 |
| 15 | ln | 链接文件 | 未完成 | 最后再做 |

## 第一个新里程碑

建议把下一个里程碑定义为：

```text
format
mount
touch /a.txt
open /a.txt w
write fd hello
close fd
open /a.txt r
read fd
close fd
exit
mount
cat /a.txt
```

验收结果：

```text
第一次 read 能得到 hello。
重新 mount 后 cat /a.txt 仍然得到 hello。
open_file_table 在 close 后清理正确。
```

## 当前结论

下一步最应该做的是：

```text
先补文件描述符运行态，再补用户运行态，最后扩展命令。
```

也就是说，优先级不是：

```text
继续堆 cp / mv / vim / chmod
```

而是：

```text
open / close / fd -> login / logout / current_user -> permissions -> more commands
```
