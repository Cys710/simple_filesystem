# FMS 功能路线整理

## 当前判断

FMS 现在已经具备一个较完整的教学型文件系统核心：

```text
格式化
挂载
多目录
文件读写
fd 打开文件表
内存 inode 表
共享读锁与独占写锁
追加与 seek
删除与资源回收
用户登录和切换
用户 home 目录
简化权限控制
```

当前阶段不再建议优先补底层能力，而应围绕“更像真实文件系统的命令体验”和“课设演示闭环”继续完善。

## 当前实现情况

### 核心能力

| 能力 | 当前情况 | 说明 |
| --- | --- | --- |
| format | 已完成 | 创建磁盘镜像并初始化 root 用户 |
| mount | 已完成 | 读取超级块、root inode、root 目录 |
| mkdir | 已完成 | 已接入父目录写权限检查 |
| touch | 已完成 | 已接入父目录写权限检查 |
| ls | 已完成 | 已接入目录 r/x 权限检查 |
| cd | 已完成 | 已接入目录 x 权限检查 |
| pwd | 已完成 | 返回当前工作目录 |
| cat | 已完成 | 复用文件读取能力 |
| write | 已完成 | 支持路径写和 fd 写 |
| append | 已完成 | 追加写入 |
| rm | 已完成 | 删除文件并释放资源 |
| rmdir | 已完成 | 支持空目录删除和递归删除 |
| open | 已完成 | 返回 fd |
| read | 已完成 | 按 fd 读取 |
| seek | 已完成 | 调整 fd offset |
| close | 已完成 | 关闭 fd |
| sync | 已完成 | 主动写回脏 inode |

### 用户和权限

| 命令 | 当前情况 | 说明 |
| --- | --- | --- |
| login | 已完成 | `login username`，密码隐藏输入 |
| logout | 已完成 | 清除当前用户 |
| whoami / who | 已完成 | 查看当前用户 |
| useradd | 已完成 | root 创建用户并自动创建 home |
| passwd | 已完成 | root 改任意用户，普通用户改自己 |
| users | 已完成 | root 查看用户列表 |
| su | 已完成 | 校验密码并切换用户 |
| chmod | 已完成 | 修改 mode，root 或 owner 可执行 |
| stat | 已完成 | 查看 inode/type/owner/mode/size |

## 权限模型说明

当前课设版本不实现 group。权限模型简化为：

```text
owner
others
mode
```

`chmod` 使用两位八进制写法：

```text
70
75
64
60
```

第一位表示 owner 权限，第二位表示 others 权限。实际判断规则是：

```text
如果当前用户是 owner，看 owner 位
否则看 others 位
root 用户绕过普通权限
未登录 guest 为兼容旧测试，暂按 root 处理
```

默认权限：

```text
普通文件：64
普通目录：75
/home/<user>：70
```

权限检查位置在 `FileSystem` 核心层，而不是 shell 层。

已检查：

```text
ls      目录 r + x
cd      目录 x
mkdir   父目录 w + x
touch   父目录 w + x
open r  文件 r
open w  文件 w
rm      父目录 w + x
rmdir   父目录 w + x
路径穿越 中间目录 x
```

## 用户系统说明

当前用户表保存在 `SuperBlock.users`，不是 `/etc/passwd`。

这样做的原因：

```text
实现简单
测试稳定
适合课设演示
避免用户系统依赖文件系统内部文件再反过来影响挂载流程
```

root 默认账号：

```text
username: root
password: 123456
```

用户创建流程：

```text
login root
useradd alice
```

执行时会通过隐藏输入提示读取密码，不需要也不应该把密码写在命令行里。

会自动创建：

```text
/home
/home/alice
```

其中 `/home/alice`：

```text
owner = alice
mode = 70
```

## fd 模型

当前已实现打开文件表：

```python
open_file_table: dict[int, OpenFile]
next_fd
```

核心 API：

```python
fs.open(path, mode) -> int
fs.read(fd, size=-1) -> bytes
fs.write(fd, data) -> int
fs.seek(fd, offset, whence=0) -> int
fs.close(fd) -> None
```

`cat/write/append` 这类路径命令可以复用 fd 能力。

## inode 注意事项

每个 inode 固定 256B：

```text
2B header
254B pickle payload
```

当前 `Inode` 使用默认 pickle 序列化，没有自定义 `__getstate__` / `__setstate__`。

去掉 group 字段后，大文件测试场景下 inode payload 约为：

```text
244B / 254B
```

还有少量余量，但后续如果继续给 inode 加字段，要注意可能再次超出 256B 槽位限制。

锁、引用计数和脏标记保存在 `MemoryInode` 中，不会写入磁盘 inode 槽位。

运行时使用 `128` 个 Hash 桶管理内存 inode。打开文件表持有 `MemoryInode`
引用：只读打开允许共享，带写能力的打开模式使用独占锁。关闭 fd 时释放锁和引用。

## 推荐演示流程

### 文件持久化

```text
format
touch /a.txt
write /a.txt hello
cat /a.txt
exit
mount
cat /a.txt
```

### fd

```text
format
open /a.txt w
write 3 hello
close 3
open /a.txt r
read 4
close 4
```

### 用户和权限

```text
format
login root
useradd alice
useradd bob
su alice
write private.txt secret
su bob
cat /home/alice/private.txt
```

预期：

```text
permission denied
```

开放只读访问：

```text
su alice
chmod 75 /home/alice
chmod 64 /home/alice/private.txt
su bob
cat /home/alice/private.txt
write /home/alice/private.txt x
```

预期：

```text
cat 成功
write permission denied
```

## 下一步建议

### 1. 完善展示型命令

优先做：

```text
tree
stat 更详细
```

这对课设演示很有帮助。

### 2. 文件管理命令

继续补：

```text
cp
mv
rename
```

这些命令可以复用现有路径解析、fd 读写、权限检查。

### 3. 缓存扩展

当前已实现 `inode_cache`、脏标记和 `sync`。后续可以继续补：

```text
dir_cache
block_cache
unmount
```

### 4. 是否迁移到 /etc/users

当前不建议优先迁移。`SuperBlock.users` 对课设演示已经足够。

如果后期必须更像 Linux，再考虑：

```text
/etc/users
/etc/passwd
/etc/shadow
```

但这会增加挂载、权限和用户系统之间的循环依赖，复杂度明显上升。

## 当前结论

当前最值得继续做的是：

```text
tree / stat 展示增强 -> cp / mv / rename -> sync / unmount
```

不建议现在引入 group。当前 owner/others 权限模型已经能满足多用户隔离和课设演示。
